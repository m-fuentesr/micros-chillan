import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.microschillan.app',
  appName: 'Gestor de flotas',
  webDir: 'dist/frontend/browser'
};

export default config;
