<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <!-- Fondo blanco con bordes redondeados (rounded-xl) -->
  <rect width="64" height="64" rx="14" fill="#ffffff"/>
  <!-- Sombra sutil -->
  <rect width="64" height="64" rx="14" fill="none" stroke="rgba(0,0,0,0.05)" stroke-width="1"/>
  <!-- Texto GF en azul primario (similar al logo del login) -->
  <text x="32" y="44" font-family="'Barlow', -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif" font-weight="800" font-size="28" text-anchor="middle" fill="#3b82f6" letter-spacing="0.5">GF</text>
</svg>
